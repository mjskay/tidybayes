#' @param value The name of the output column:
#'  - for `[add_]epred_<%=pred_type%>()`, defaults to `".epred"`.
#'  - for `[add_]predicted_<%=pred_type%>()`, defaults to `".prediction"`.
#'  - for `[add_]linpred_<%=pred_type%>()`, defaults to `".linpred"`.
#' <%=if(pred_type == "draws") ' - for `[add_]residual_draws()`, defaults to `".residual"`'%>
